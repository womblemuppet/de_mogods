//  (type is chosen in spawn_instrument_block)


with instance_place(x,y,block)
	instance_destroy()
    
dontmakeorb=false   ///if enabled won't spawn orb on death (eg if pocketed)

event_inherited()

