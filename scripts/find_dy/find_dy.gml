/***************************************************
  finds the dx for player movement from directional code
****************************************************/

if argument0==0 || argument0==2
return 0
if argument0==1
return -Spd
if argument0==3
return Spd

